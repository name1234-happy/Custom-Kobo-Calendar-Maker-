# Kobo Calendar Maker v4 — Fixed

**Export PDF** now generates a PDF instead of showing the old placeholder message. Calendar date boxes link to the corresponding daily pages.

**Save Design** creates an editable JSON file. **Load Design** or the upload box restores that JSON so you can continue editing later.

Open `index.html` in a browser or host the files on GitHub Pages.
